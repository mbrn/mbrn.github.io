import * as React from 'react'

export interface EducationProps {
  resumeData: any;
}

export default class Education extends React.Component<EducationProps, undefined> {
  render() {
    return (
      <span>
        Education
      </span>
    );
  }
}